Hey You GAY  NIKHIL. Here are your Slip codes:
Only Q1 are solved below!!

---
QUESTIONS WHICH ARE SOLVED IN ORDER: 
Write a program to accept a number from user and generate multiplication table of a 
number. Accept the number using command line argument. 

Write a program to read the First Name and Last Name of a person, his weight and height 
using Scanner class. Calculate the BMI Index which is defined as the individual's body mass 
divided by the square of their height.  
(Hint : BMI = Wts. In kgs / (ht)2 ) 

Write a program to accept the 'n' different numbers from user and store it in array. Print the 
sum of elements of the array

Write a program to accept the user name and greets the user by name. Before displaying 
the user's name, convert it to upper case letters. For example, if the user's name is Raj, then 
display greet message as "Hello, RAJ, nice to meet you!".  

 Write a program to accept a number from the user using BufferedReader class. Display 
factorial of a number.  

Write a program to display the Employee (Empid, Empname, Empdesignation, Empsal) 
information using toString(). 

Create a class Sphere, to calculate the volume and surface area of sphere. Accept the input 
using command line argument. 
(Hint : Surface area=4*3.14(r*r), Volume=(4/3)3.14(r*r*r))  

Write a program to reverse a number. Accept number using BufferedReader class. 

 Write a program to accept a number from user. Check whether number is perfect or not. 
Use Scanner class for accepting input from user. 

 Write a program to accept a number from user. Check whether number is prime or not. 
Accept the number using command line argument.   

Write a program create class as MyDate with dd,mm,yy as data members. Write 
parameterized constructor. Display the date in dd-mm-yy format. (Use this keyword)

Write a program to accept ‘n’ names of countries and display country names in capital 
letters.

Write a Java program to display prime numbers in the range from 1 to 100.

Write a program to accept a number from user. Check whether number is armstromg or not. 

Write a program to print the factors of a number. Accept a number using BufferedReader 
class.  

Create a class Student (rollno, name ,class, per) to read student information using 
BufferedReader class  and display details. 

Write a program to Calculate the volume of the cylinder. (Hint : Volume: π × r2 × h) 
Accept the input using Command line argument. 

 Write a program to accept the 'n' different numbers from user and store it in array. Display 
minimum number from an array.

Write a program to accept the 'n' different numbers from user and store it in array. Display 
maximum number from an array. 

Write a program to accept 3 numbers using command line argument. Sort and display the 
numbers.  

Write a program to accept a filename from user. Check whether file exist or not. If it exist 
then its size and last modified time.  

Write a proram to accept two string using Scanner class. Check whether two strings are 
equal or not. 

Write a program to create class Person (pname, pmobno). Create two objects of Person class. 
Display hashcode of these two objects and check whether hashcode are equal or not. 

Write a program to display sum of digits of a number. Accept number using command line 
argument. 

 Write a program to display count the number of digits of a number. Accept number using 
BufferedReader class.  

Solutions:
### **1. Multiplication Table (Command Line Argument)**

```java
class Table {
    public static void main(String args[]) {
        int n = Integer.parseInt(args[0]);
        for(int i=1;i<=10;i++) {
            System.out.println(n + " x " + i + " = " + (n*i));
        }
    }
}
```

---

### **2. BMI using Scanner**

```java
import java.util.*;

class BMI {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        String fname = sc.next();
        String lname = sc.next();
        double weight = sc.nextDouble();
        double height = sc.nextDouble();
        double bmi = weight / (height * height);
        System.out.println("BMI = " + bmi);
    }
}
```

---

### **3. Sum of Array**

```java
import java.util.*;

class SumArray {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int arr[] = new int[n];
        int sum = 0;
        for(int i=0;i<n;i++) {
            arr[i] = sc.nextInt();
            sum += arr[i];
        }
        System.out.println("Sum = " + sum);
    }
}
```

---

### **4. Greeting in Uppercase**

```java
import java.util.*;

class Greet {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        String name = sc.next();
        System.out.println("Hello, " + name.toUpperCase() + ", nice to meet you!");
    }
}
```

---

### **5. Factorial using BufferedReader**

```java
import java.io.*;

class Factorial {
    public static void main(String args[]) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int fact = 1;
        for(int i=1;i<=n;i++) {
            fact = fact * i;
        }
        System.out.println("Factorial = " + fact);
    }
}
```

---

### **6. Employee using toString()**

```java
class Employee {
    int id;
    String name, desig;
    double sal;

    Employee(int id, String name, String desig, double sal) {
        this.id = id;
        this.name = name;
        this.desig = desig;
        this.sal = sal;
    }

    public String toString() {
        return id + " " + name + " " + desig + " " + sal;
    }

    public static void main(String args[]) {
        Employee e = new Employee(1,"Raj","Manager",50000);
        System.out.println(e);
    }
}
```

---

### **7. Sphere (Command Line Argument)**

```java
class Sphere {
    public static void main(String args[]) {
        double r = Double.parseDouble(args[0]);
        double area = 4 * 3.14 * r * r;
        double vol = (4.0/3) * 3.14 * r * r * r;
        System.out.println("Area = " + area);
        System.out.println("Volume = " + vol);
    }
}
```

---

### **8. Reverse Number (BufferedReader)**

```java
import java.io.*;

class Reverse {
    public static void main(String args[]) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int rev = 0;
        while(n>0) {
            rev = rev*10 + n%10;
            n = n/10;
        }
        System.out.println("Reverse = " + rev);
    }
}
```

---

### **9. Perfect Number (Scanner)**

```java
import java.util.*;

class Perfect {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int sum = 0;
        for(int i=1;i<n;i++) {
            if(n%i==0) sum += i;
        }
        if(sum==n) System.out.println("Perfect");
        else System.out.println("Not Perfect");
    }
}
```

---

### **10. Prime (Command Line)**

```java
class Prime {
    public static void main(String args[]) {
        int n = Integer.parseInt(args[0]);
        int flag = 0;
        for(int i=2;i<=n/2;i++) {
            if(n%i==0) flag=1;
        }
        if(flag==0) System.out.println("Prime");
        else System.out.println("Not Prime");
    }
}
```

---

### **11. MyDate Constructor**

```java
class MyDate {
    int dd,mm,yy;

    MyDate(int dd,int mm,int yy) {
        this.dd = dd;
        this.mm = mm;
        this.yy = yy;
    }

    public static void main(String args[]) {
        MyDate d = new MyDate(10,3,2026);
        System.out.println(d.dd + "-" + d.mm + "-" + d.yy);
    }
}
```

---

### **12. Countries Uppercase**

```java
import java.util.*;

class Country {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        String arr[] = new String[n];
        for(int i=0;i<n;i++) {
            arr[i] = sc.next();
            System.out.println(arr[i].toUpperCase());
        }
    }
}
```

---

### **13. Prime 1 to 100**

```java
class Prime100 {
    public static void main(String args[]) {
        for(int i=2;i<=100;i++) {
            int flag=0;
            for(int j=2;j<=i/2;j++) {
                if(i%j==0) flag=1;
            }
            if(flag==0) System.out.println(i);
        }
    }
}
```

---

### **14. Armstrong Number**

```java
import java.util.*;

class Armstrong {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int temp=n,sum=0;
        while(n>0) {
            int r=n%10;
            sum += r*r*r;
            n=n/10;
        }
        if(sum==temp) System.out.println("Armstrong");
        else System.out.println("Not Armstrong");
    }
}
```

---

### **15. Factors (BufferedReader)**

```java
import java.io.*;

class Factors {
    public static void main(String args[]) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        for(int i=1;i<=n;i++) {
            if(n%i==0) System.out.println(i);
        }
    }
}
```

---

### **16. Student (BufferedReader)**

```java
import java.io.*;

class Student {
    int roll;
    String name,cls;
    double per;

    public static void main(String args[]) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        Student s = new Student();
        s.roll = Integer.parseInt(br.readLine());
        s.name = br.readLine();
        s.cls = br.readLine();
        s.per = Double.parseDouble(br.readLine());
        System.out.println(s.roll+" "+s.name+" "+s.cls+" "+s.per);
    }
}
```

---

### **17. Cylinder Volume (Command Line)**

```java
class Cylinder {
    public static void main(String args[]) {
        double r = Double.parseDouble(args[0]);
        double h = Double.parseDouble(args[1]);
        double v = 3.14 * r * r * h;
        System.out.println("Volume = " + v);
    }
}
```

---

### **18. Minimum in Array**

```java
import java.util.*;

class MinArray {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int arr[] = new int[n];
        for(int i=0;i<n;i++) arr[i]=sc.nextInt();
        int min = arr[0];
        for(int i=1;i<n;i++) {
            if(arr[i]<min) min=arr[i];
        }
        System.out.println("Min = " + min);
    }
}
```

---

### **19. Maximum in Array**

```java
import java.util.*;

class MaxArray {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int arr[] = new int[n];
        for(int i=0;i<n;i++) arr[i]=sc.nextInt();
        int max = arr[0];
        for(int i=1;i<n;i++) {
            if(arr[i]>max) max=arr[i];
        }
        System.out.println("Max = " + max);
    }
}
```

---

### **20. Sort 3 Numbers (Command Line)**

```java
class Sort3 {
    public static void main(String args[]) {
        int a = Integer.parseInt(args[0]);
        int b = Integer.parseInt(args[1]);
        int c = Integer.parseInt(args[2]);
        int temp;
        if(a>b){ temp=a;a=b;b=temp; }
        if(b>c){ temp=b;b=c;c=temp; }
        if(a>b){ temp=a;a=b;b=temp; }
        System.out.println(a+" "+b+" "+c);
    }
}
```

---

### **21. File Check**

```java
import java.io.*;

class FileCheck {
    public static void main(String args[]) {
        File f = new File(args[0]);
        if(f.exists()) {
            System.out.println("Size: " + f.length());
            System.out.println("Last Modified: " + f.lastModified());
        } else {
            System.out.println("File not found");
        }
    }
}
```

---

### **22. String Equal (Scanner)**

```java
import java.util.*;

class StringEqual {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        String s1 = sc.next();
        String s2 = sc.next();
        if(s1.equals(s2)) System.out.println("Equal");
        else System.out.println("Not Equal");
    }
}
```

---

### **23. Person HashCode**

```java
class Person {
    String name;
    String mob;

    Person(String n,String m) {
        name=n;
        mob=m;
    }

    public static void main(String args[]) {
        Person p1 = new Person("A","111");
        Person p2 = new Person("B","222");
        System.out.println(p1.hashCode());
        System.out.println(p2.hashCode());
        if(p1.hashCode()==p2.hashCode())
            System.out.println("Equal");
        else
            System.out.println("Not Equal");
    }
}
```

---

### **24. Sum of Digits (Command Line)**

```java
class SumDigits {
    public static void main(String args[]) {
        int n = Integer.parseInt(args[0]);
        int sum = 0;
        while(n>0) {
            sum += n%10;
            n = n/10;
        }
        System.out.println("Sum = " + sum);
    }
}
```

---

### **25. Count Digits (BufferedReader)**

```java
import java.io.*;

class CountDigits {
    public static void main(String args[]) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int count = 0;
        while(n>0) {
            count++;
            n = n/10;
        }
        System.out.println("Digits = " + count);
    }
}
```

---
